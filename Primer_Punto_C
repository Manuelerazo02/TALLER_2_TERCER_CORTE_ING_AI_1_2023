#include <stdio.h>

int main() {
    float pi = 3.141592;
    float radio = 14.5;
    float altura = 26.79;

    float volumen = (1.0 / 3.0) * pi * radio * radio * altura;

    printf("El volumen del cono es: %.2f\n", volumen);

    return 0;
}
